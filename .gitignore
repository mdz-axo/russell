/target
**/*.rs.bk
Cargo.lock.bak
.DS_Store
.idea/
.vscode/
*.swp

# Secrets — never commit
.env
.env.*
!.env.example
!.env.template

# Archive — git history is canonical per TOGAF-Lite;
# docs/archive/ is a local convenience copy only.
docs/archive/
