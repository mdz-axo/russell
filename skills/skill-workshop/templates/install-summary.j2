{# skill-workshop: install summary #}
{# Summary after installing a skill #}

## Skill Installed ✓

**Skill ID:** {{ probes["installed-skill-id"] | default(params.skill_id | default("unknown")) }}
**Version:** {{ probes["installed-version"] | default("unknown") }}
**State:** Active

### What Was Installed

{% if probes["installed-probes"] %}
**Probes ({{ probes["probe-count"] }}):**
{{ probes["installed-probes"] }}
{% endif %}

{% if probes["installed-interventions"] %}
**Interventions ({{ probes["intervention-count"] }}):**
{{ probes["installed-interventions"] }}
{% endif %}

### Symptom Coverage

{% if probes["covers-symptoms"] %}
This skill handles: {{ probes["covers-symptoms"] }}
{% endif %}

### Next Steps

1. **Test probes:**
   ```
   run-probe {{ probes["installed-skill-id"] | default(params.skill_id | default("")) }} <probe-id>
   ```

2. **Update registry cache:**
   ```
   rebuild-cache
   ```

3. **Verify coverage:**
   ```
   coverage
   ```

---
*Skill is now active. Want to test a probe or return to workshop menu?*