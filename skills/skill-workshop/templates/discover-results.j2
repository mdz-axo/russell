{# skill-workshop: discover results #}
{# Displays skill discovery search results #}

## Skill Discovery Results

**Query:** {{ params.symptom | default("unknown") }}
**Source:** {{ probes["search-source"] | default("local registry + web") }}

### Matching Skills Found

{% if probes["matching-skills"] %}
{{ probes["matching-skills"] }}
{% else %}
No matching skills found in the local registry.

### Web Search Results
{% if probes["web-results"] %}
{{ probes["web-results"] }}
{% else %}
No web results available. Consider building a new skill for this symptom.
{% endif %}
{% endif %}

### Recommended Actions

1. **Install existing skill:**
   ```
   install <skill-id>
   ```

2. **Build new skill (if none found):**
   ```
   build {{ params.symptom | default("new-skill") }}-watcher
   ```

3. **Search with different terms:**
   ```
   discover <related-symptom>
   ```

---
*Found {{ probes["result-count"] | default("0") }} matching skills. Want to evaluate or install one?*