{# skill-workshop: evaluate pass #}
{# Displayed when skill evaluation passes #}

## Skill Evaluation: Pass ✓

**Skill ID:** {{ probes["skill-id"] | default(params.skill_id | default("unknown")) }}
**Version:** {{ probes["skill-version"] | default("unknown") }}
**Kind:** {{ probes["skill-kind"] | default("actionable") }}

### Validation Results

✓ Manifest schema valid
✓ Symptom names recognized
✓ Scripts referenced correctly
✓ IDRS contract satisfied
✓ Safety scanner passed

### Skill Summary

**Description:** {{ probes["skill-description"] | default("No description") }}

**Probes:** {{ probes["probe-count"] | default("0") }}
{% if probes["probe-list"] %}
{{ probes["probe-list"] }}
{% endif %}

**Interventions:** {{ probes["intervention-count"] | default("0") }}
{% if probes["intervention-list"] %}
{{ probes["intervention-list"] }}
{% endif %}

**Max auto risk:** {{ probes["max-auto-risk"] | default("low") }}

### Recommendation

✓ **Ready for installation**

Install with:
```
install {{ probes["skill-id"] | default(params.skill_id | default("")) }}
```

---
*Skill passed all validation checks. Safe to install.*